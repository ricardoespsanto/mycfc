# Acceptance evidence matrix

This is the phase-10 status for the implemented application slice. A row is only marked covered where an executable test exists; manual checks and unavailable environments remain explicit.

| Workflow | Unit evidence | Integration evidence | Browser evidence | Status / gap |
|---|---|---|---|---|
| Privacy request intake, review and executable decision plan (#110/#243) | `internal/privacyrequests` lifecycle, policy, plan and delivery tests plus handler, SMTP/outbox and access-log tests cover exact adopted scopes, closed category-purpose/profile/action/schema/anchor versions, unknown category/profile/field rejection, unresolved future anchors, complete partial/refusal plans, deterministic ordering, request-bound digest verification, independent reviewer access, current representation, recent authentication, stale forms, generic encrypted notices, privacy-safe logs and native-form security headers | `TestPrivacyServiceTransactions` covers atomic case/plan/event/outbox rollback, persisted 32-byte plan digest, plan-derived category-scoped retention exceptions, scheduled working scrub despite a category hold, active-hold deletion prevention, immutable plan retention through working-data scrub and deletion at evidence expiry, idempotent receipt, concurrent claims/decisions, revoked reviewers, credential/throttle boundaries, current guardian authority, dependant/last-administrator safeguards and preservation of open cases; the forward-migration integration test preserves representative legacy rows and executes expiry with and without an active hold, while baseline tests enforce the migration cutoff | `e2e/privacy-requests.spec.mjs` covers adult, closure, partial/refusal, authorization, guardian/minor and no-JavaScript journeys with Mailpit, keyboard focus, 320px reflow and serious/critical axe checks | Implemented behind a disabled-by-default application gate; decision approval alone preserves account access and never claims erasure is complete |
| Privacy execution control plane (#244) | Lifecycle, exact execution-graph/replay validation, session-store, config, authentication/navigation, handler/page, notification, executor-grant, fencing and retry tests cover independent executor capability, four-eyes separation, explicit confirmation, a 12-hour session ceiling, safe blocker presentation, generic processing notice, immutable plan/checkpoint binding and open processing/failure states | `TestPrivacyServiceTransactions` covers atomic category handoff and exact replay without access cutoff, changed-fact fail-closed checks including dependants added after approval, concurrent starts, concurrent administrator grant/revocation/deactivation versus closure start, late-fault rollback, dependant closure bound to the exact durable execution, last-usable-admin and legacy-session refusal, indexed-session/account/token/role cutoff, two-worker exclusive claims, heartbeat, expired-lease recovery, stale-worker rejection, checkpoints, success-without-request-completion and retry exhaustion. Database tests cover inactive-subject attachment races, effective web/worker privileges with no direct worker DML, atomic migration-plus-hardening, and representative #243-to-#244 forward migration without invented executions | `e2e/privacy-requests.spec.mjs` covers a separately authenticated executor reviewing bounded blockers at 320px, keyboard confirmation and category-processing start with JavaScript disabled, while proving the requester stays authenticated; Mailpit and serious/critical axe checks remain active | Control plane covered but production-inactive. #248 must supply destructive category handlers, completion-evidence verification, worker deployment credentials/service and separate production activation; `SUCCEEDED` execution remains an open request and is not presented as completed |
| Relational privacy execution foundation (#245) | Executor tests cover the closed operation allowlist, unsupported-operation refusal before access cutoff, dependency-safe category ordering, exact lease/attempt fencing, ordered checkpoints and idempotent replay | PostgreSQL tests reconcile every direct `users(id)` foreign key against the reviewed inventory, enforce tombstone/profile/write and worker-privilege boundaries, exercise the end-to-end tombstone path and selected destructive handlers with atomic postcondition and forced-rollback checks, apply the exact forward migration over the preceding schema, and prove membership revocation/pseudonymisation preserves historical IDs and unrelated rows without a reverse identity map while blocking late membership attachment. Selected audit-groundwork fixtures prove fencing, idempotency, recursive current-identifier canary scrubbing and pseudonymous read visibility | Existing privacy and profile journeys remain green; production execution remains disabled, so no browser journey claims completed erasure | Partially covered and fail closed. Identity/profile/access, subject-owned deletion, repair-reporter anonymisation and membership-history pseudonymisation are implemented. Per-handler seeded evidence is not yet exhaustive. Audit-actor groundwork remains outside the production allowlist until all provenance references and historical aliases are covered; retained/expiring records, cross-store execution, final case completion and production activation remain later work |
| Cross-store privacy execution foundation (#246) | Storage tests cover exact-key filtering, paginated version/delete-marker collection, explicit version deletion, stable-absence proof, concurrent-version refusal, bounded failure and opaque errors; crypto/coordinator tests cover reserve-before-PUT ordering, randomized upload-specific envelopes, immutable-context authentication, ambiguous PUT handling, bounded cleanup retries, tamper/wrong-key rejection and separately keyed evidence; handler tests prove all three writes require provenance and no longer issue ordinary compensation deletes; ordinary/tagged command tests prove separate compile-time purge gates and protected file-based evidence keys; Terraform contract tests prove the temporary purge identity is absent by default, inventory and deletion are separately gated, all permissions expire, and the identical boundary contains only exact actions/prefixes | MinIO coverage exercises the upload coordinator through the cleanup worker and deletes every exact-key version and delete marker with stable absence; PostgreSQL coverage proves profile, repair and equipment attachment, exact locator/content/size commitments, deferred commit-time event and pointer-table invariants, direct pointer-tamper/removal rejection, null-token and standalone-attach rejection, replacement cleanup, source mismatch, concurrent replacement single-winner behavior, 24-hour stale handling, active-hold exclusion, atomic execution-start target capture before status/access changes, source locking before classification/counting, reservation-to-finalisation serialization and post-capture rejection, single-worker claims, expired-lease fencing, stable-absence evidence, privilege-drift repair, direct protected-table denial and separate web/worker routine grants. Forward audit and upload-foundation migrations remain covered | Existing privacy, profile, repair and fleet journeys remain green; no browser test claims cross-store erasure because keys, worker and production execution remain disabled | Partially covered and production-inactive. Configured new uploads reserve, seal, confirm and atomically attach protected provenance; failed, ambiguous, superseded, removed and stale intents converge through an unprovisioned version-aware cleanup worker API. Missing keys fail photo writes closed. Legacy pointers remain readable but replacement/removal is blocked until the separately approved purge or verified backfill, preventing their only locator from being orphaned. Execution start atomically captures exact protected targets under the shared subject/source locks before status transition or access cutoff and rejects new in-scope uploads after commit. A purge-only immutable image and hardened host wrapper are source-ready but cannot be selected by normal deployment; no temporary identity, key, published purge image, permissions, execution, database reset, or activation exists. Retained-object rewriting and any future provider-specific work remain blocked. |
| External provider/recipient erasure slice (#246) | `provider_execution_test.go` covers empty/invalid/duplicate closed registries, randomized target and credential envelopes, immutable binding/tamper rejection, distinct key roles, privacy-safe secret formatting, bounded retry/exhaustion, already-disconnected idempotency, evidence transcript fencing, and processor rejection plus strict allowlists for `NOT_CONTROLLABLE` | `TestProviderCaptureAndEvidenceAreAtomicFencedAndPrivate` proves target/count materialisation, sync/webhook/reconnect fencing, source credential removal, sealed quarantine, inactive wearable exclusion, worker evidence, credential purge only after verified success, and absence of target/credential/receipt/subject canaries from evidence. `TestUnknownProviderRollsBackCaptureAndQuarantine` proves an unregistered active service restores the original state and credential when capture aborts | No browser evidence is claimed because provider operations, the worker and the privacy workflow remain disabled | Source framework covered and production-inactive. The signed v2 inventory is complete and empty because no subject-specific external integration is active. Any future integration requires factual registration, a real adapter, source wiring and its own activation evidence before live provider action |
| Bounded privacy retention maintenance (#247) | CLI/config and profile-store tests cover the exact disabled gate, UUID and 1–10,000 batch bounds, active consent replacement/withdrawal, and privacy-safe aggregate output contract; deployment tests cover inert and enabled wrapper paths without credential exposure; Terraform contract tests cover the repair lifecycle prefix/deadlines and immediate failure alarm | `TestPrivacyRetentionCompletesConsentAuditAndRepairClocks` covers three-year consent expiry with live-FK skipping, per-row 24-month pseudonymisation while preserving a newer row, day-23 repair queuing, pointer clearing, overdue detection and authoritative absence clearance. `TestPrivacyRetentionRejectsConcurrentRuns` covers the single-run fence; baseline and forward migration checks preserve legacy rows | No browser surface exists; the bounded timer is an operator-only source artifact and remains disabled | Source covered and production-inactive. The fixed capability role has no login or direct table access. Dedicated login/secret provisioning, Terraform apply, timer activation, legacy purge, deployment and privacy-request activation remain separate gates |
| Restore-independent erasure replay (#247) | Tombstone/CLI tests cover locator-bound authenticated discovery, strict legacy compatibility, current closure-v4 emission, distinct erasure/closure clocks, authenticated membership-history postcondition digests, bounded inventory/result contracts, exact schema digesting, synthetic/live separation and privacy-safe failures | PostgreSQL coverage replays the closed relational allowlist plus local provider fencing, preserves unrelated rows, proves idempotency and tamper/conflict refusal, independently verifies an exact source erasure in a post-erasure backup with immutable `ALREADY_APPLIED` evidence, detects old identity/date/missing/extra/nested-JSON membership-history corruption while proving canonical row/JSON ordering and principal exclusion, exercises the synthetic fixture through the ordinary CLI, checks the additive forward migration, and enforces the observer's no-table/fixed-function role | No browser surface exists; replay is an isolated operator path with no S3/provider clients | Source covered and production-inactive. Current evidence requires closure v4, original effective-time equality, exact membership-history digest/count equality, exact succeeded checkpoint coverage, provider absence, consent-clock equality and independently bound observer aggregates. Closure v1-v3 remains diagnostic-only and cannot cross the replay database boundary. Live credentials, oldest-backup drill, outer signed attestation, promotion-gate activation and deployment remain separate gates |
| Adult registration, login, session, consent validation | `internal/handlers/registration_test.go`, `login_test.go` | `internal/handlers/integration_test.go:TestPostgresRegistrationStorePersistsRequiredTermsConsentAtomically` | `e2e/auth.spec.mjs` registration and interactive login | Covered |
| Public legal information and optional sensitive-data consent | `internal/handlers/legal_test.go`, router, component, registration and profile tests cover the seven allowlisted current routes, representative immutable versioned routes, and both privacy versions with their exact digests, shared legal navigation, optional profile-photo authorization and explicit adult health-data consent | `TestPostgresProfileStoreEnforcesGuardianConsentConflictAndAudit` covers atomic health-consent evidence, refusal without consent or with a different hash, administrator minimization, majority cutoff and purpose-specific profile-photo consent | The legal page is covered at mobile/desktop widths; authentication and authenticated layouts expose the shared navigation; registration proves image authorization is absent and the photo flow requires it | Covered at the implemented points; verified authority for a representative to grant special-category/image consent remains deferred, so those grants are limited to the adult titular in this release |
| Password recovery | `internal/passwordreset/service_test.go`, `internal/emailverification/smtp_test.go`, `worker_test.go`, `internal/handlers/password_recovery_test.go`, auth/session tests, and router/security-header tests cover generic outcomes, privacy-safe operational events, trusted-client limiting, validation, replay, one-time confirmation, credential-version rejection, CSRF, anonymous-only routing, and privacy headers | PostgreSQL coverage exercises concurrent issuance, outbox creation, exactly-once consumption, bcrypt replacement, credential-version increments, old-session rejection, resend limits, administrator/minor credential replacement, and eligibility cancellation | `e2e/auth.spec.mjs` follows the Mailpit link, checks validation without password repopulation, resets through the browser flow, rejects the old password/session/used link, accepts the new password, and covers generic unknown/`CFC-`, axe, keyboard, and 320px states | Covered; operator diagnosis is documented in `docs/password-recovery-operations.md` |
| Guardian dependants and minor credentials | `guardian_test.go`, `members_test.go`, `auth_test.go` | `TestPostgresGuardianDependentStorePersistsResponsibilityAndEnforcesLimit`, `TestMinorCredentialRequiresCurrentGuardianAndWritesAudit` | `e2e/auth.spec.mjs` dependant creation through the browser flow | Covered; minor credential issuance/recovery has no browser flow |
| Role workspaces and access control | `auth_test.go`, `dashboard_test.go` | `internal/db/integration_test.go:TestMembershipsResolveActiveSportStructureAndRejectMismatches` | Registration dashboard redirect and administrator fleet in `e2e/auth.spec.mjs` | Covered at unit/integration; coach/moderator browser journeys remain absent |
| Member management | `members_test.go` | None | Administrator creates an adult and assigns a competition membership in `e2e/auth.spec.mjs` | Covered for adult creation and membership assignment; dependant credentials and deactivation lack browser evidence |
| Private member profiles | `profile_test.go` covers authorization, validation, metadata-only audit fields, initials fallback, FPC number validation, allowlisted national/international history URL derivation, and the explicit destructive photo-removal confirmation | `TestPostgresProfileStoreEnforcesGuardianConsentConflictAndAudit`, `TestMemberProfileOptimisticUpdateAndImmutableAudit`; MinIO object lifecycle remains covered by `internal/storage/integration_test.go` | Adult editing, profile completion, consent-backed photo upload and removal confirmation, administrator FPC assignment followed by athlete-visible official-history links, responsive/axe checks, and guardian dependant editing in `e2e/auth.spec.mjs` | Covered for self/guardian editing, consent, storage compensation, stale-write protection, sensitive audit metadata, authenticated avatars, inactive-member restrictions, destructive self-photo removal, and numeric-only FPC outbound links without profile scraping; dependant read-only, guardian/administrator photo-removal journeys, and administrator identity correction outside the FPC field remain without dedicated browser journeys |
| Events | `events_test.go` | `TestListEventsForTodayRespectsMembershipCoachGrantAndAdminVisibility`, `TestEventEditAndCancellationPreserveResponsesAndRejectStaleWrites` | Administrator authoring, editing/cancellation, RSVP, capacity/waitlist, confirmation, and check-in in `e2e/auth.spec.mjs` | Source and PostgreSQL coverage includes lifecycle, concurrency, preserved responses, and cancelled visibility; coach and guardian browser journeys remain absent |
| Announcements and official documents | `announcements_test.go` | None | Targeted publication, first-read delivery, and expiry in `e2e/auth.spec.mjs` | Covered for administrator and competition-athlete lifecycle; coach and official-document browser journeys remain absent |
| Member suggestions | `suggestions_test.go`, `auth_test.go` cover validation, requester identity, requester-scoped rendering, staff authorization, terminal responses, pagination filters, and stale-write handling | `TestSuggestionsRemainRequesterScopedAndUseOptimisticTriage` | `e2e/auth.spec.mjs` covers member submission, administrator triage, requester-visible response, axe, and an interactive member page | Covered for the bounded private suggestion lifecycle; dedicated moderator browser evidence remains absent |
| Feature availability | `internal/featureflags/featureflags_test.go`, `auth_test.go`, `dashboard_test.go`, and `feature_flags_test.go` cover registry defaults, the disabled/administrator-only/enabled matrix, fail-closed reads, navigation, validation, and stale writes | `TestFeatureFlagsUseDefaultsConcurrencyAndImmutableAudit` covers seeded defaults, optimistic updates, preserved feature data, repair of a missing registered row, and append-only actor audit | `e2e/feature-flags.spec.mjs` changes only the dormant photo-submission setting, verifies all modes and audit output, and restores the disabled default | Covered for registered server-rendered routes; Suggestions is enforced end-to-end and future slices must explicitly apply their registered guard |
| Private photo albums | `photo_albums_test.go`, `auth_test.go` cover scoped list rendering, explicit audience validation, moderator authoring, fail-closed detail, versioned archive, and the deliberate absence of upload controls | `TestPhotoAlbumIntegrationScopesLifecycleAndAudit` covers member, named-guardian, coach, unrelated-user, privileged, archive, required-audience, and immutable-audit behavior | `e2e/auth.spec.mjs` creates and archives an album, checks scoped athlete visibility and axe, and confirms that no upload control exists | Covered for the #149 album foundation only; consent-aware upload, moderation, private media delivery, reporting, removal, and retention remain #150-#152 |
| News | `news_test.go`, `dashboard_test.go` | None | Administrator draft/publish/expire and leisure-workspace visibility in `e2e/auth.spec.mjs` | Covered for past scheduled publication; future-time automatic publication remains untested |
| Repair reporting and status transitions | `repair_test.go`, `dashboard_test.go` | `TestCreateRepairRequestEnforcesIdempotencyKeyDuringConcurrentRetries` | `e2e/auth.spec.mjs` photo upload and same-key retry | Covered for hostile invalid upload, cross-user retry, database cleanup, concurrent duplicate creation and stale status conflict; administrator resolution lacks browser evidence |
| Maintenance scheduling and completion | `dashboard_test.go` | `TestScheduleMaintenanceTaskUpdatesOnlyDueEquipment` | `e2e/auth.spec.mjs` administrator validation, keyboard scheduling and completion | Covered |
| Training, structured planning, private publication, activity integration foundation, and distance leaderboard | `training_test.go`, `structured_training_test.go`, `ui/pages/structured_training_test.go`, `dashboard_test.go`, `internal/activity/provider_test.go` | `TestTrainingSessionEditingAndCancellationLifecycle`, `TestPostgresTrainingPublicationsPreservePrivateRevisionLineage`, `TestPostgresStructuredTrainingCyclesRemainScopedVersionedAndCopyIndependentDrafts`, structured group/week/season/hybrid hierarchy and guardian visibility integration coverage, `TestDistanceLeaderboardEnforcesRankingPrivacyAndOwnership`, `TestActivityIntegrationPersistsIdempotentlyAndOwnsMatches`, `TestActivityMigrationPreservesExistingRecords` | `e2e/auth.spec.mjs` covers ordinary plan/session authoring, future-session editing/cancellation, cancelled athlete visibility, athlete actual-duration/distance/perception reporting and correction, Today ranking/privacy, an administrator creating a season-labelled hybrid gym/water week, and the focused exact-preview → immutable publication → private detail journey with axe and 320px checks; responsive leaderboard states remain in `e2e/ui-review.spec.mjs`. | Covered for optional multi-week cycle creation/editing, scoped ordered-week assignment, competition targets, optimistic concurrency, honest load/modality coverage warnings, and independent deep copy without publications, outcomes or targets. Structured group/week/session/segment/block persistence, explicit season association, hybrid segment ordering, variation precedence/conflicts/omission, immutable per-athlete publication revisions, exact material-change preview, self/current-minor-guardian/current-coach authorization, stale publication rejection, historical readability after membership/group changes, outcome-to-prescription lineage, version-safe feedback correction, and exact-revision feedback visibility are also covered. Structured water prescriptions, revisioned intensity profiles, reusable routines, and provenance-labelled planned totals are covered; cycle authoring currently has handler/component/accessibility semantics but no dedicated Playwright journey. The multiple-minor guardian chooser has unit rendering/authorization evidence but no dedicated Playwright journey. Athlete feedback is covered in the focused publication journey and privacy rules are documented in `docs/training-feedback-privacy.md`; wearable-derived planned/realized load remains a separate ticket; provider-specific OAuth/sync, automatic matching, and Today confirmation UI remain separate work; replacement-session persistence still lacks browser evidence. |
| Validation and error responses | Handler tests including `login_test.go`, `registration_test.go`, `repair_test.go`, `dashboard_test.go` | Transaction rollback in registration integration test | Registration, maintenance and repair validation paths in `e2e/auth.spec.mjs` | Covered for implemented forms; error-summary focus still needs manual verification |
| Responsive, keyboard and accessibility | Component and handler rendering tests, including equipment validation, private photo upload and stale-edit recovery | Equipment CRUD audit, retirement cancellation, history preservation and reactivation in `internal/db/integration_test.go`; MinIO upload/delete in `internal/storage/integration_test.go` | `e2e/accessibility-states.spec.mjs` covers representative public empty/invalid, member empty, 403/404, administrator populated, and closed/open authoring states with structural and axe checks; `e2e/auth.spec.mjs` retains keyboard, responsive, zoom, upload and interactive journeys | Representative page-state regression coverage is in normal CI; screen-reader, contrast, exhaustive focus order, calendar fallback and non-representative route/data combinations remain manual per `docs/accessibility-manual-check.md` |

## Commands

| Evidence layer | Command | Environment |
|---|---|---|
| Unit | `make test` | Local Go toolchain |
| Database and object-storage integration | `make test-integration` | Docker, `.env`, PostgreSQL and MinIO |
| Browser and axe | `make test-e2e` | Docker, `.env`; starts the pinned Playwright container and seeded local stack |

`make verify-foundation` remains the faster deterministic focused gate. `make verify` runs the full available source gate, including generation, Go checks, browser coverage, and Terraform formatting/validation; environment-dependent and remaining acceptance gaps are still recorded below rather than treated as covered.
