// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: leaderboard.sql

package dbgen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const listDistanceLeaderboard = `-- name: ListDistanceLeaderboard :many
WITH params AS (
    SELECT $1::uuid AS current_user_id
), totals AS (
    SELECT u.id AS user_id, u.name,
           sum(o.distance_metres)::bigint AS total_metres
    FROM training_session_outcomes o
    JOIN training_sessions s ON s.id = o.session_id
    JOIN users u ON u.id = o.user_id
    WHERE o.status = 'COMPLETED'
      AND o.distance_metres IS NOT NULL
      AND u.is_active = true
      AND u.leaderboard_visible = true
      AND EXISTS (
          SELECT 1
          FROM user_memberships membership
          WHERE membership.user_id = u.id
            AND membership.starts_on <= $2::date
            AND (membership.ends_on IS NULL OR membership.ends_on >= $2::date)
      )
      AND s.starts_at <= $3::timestamptz
      AND ($4::timestamptz IS NULL OR s.starts_at >= $4::timestamptz)
      AND ($5::timestamptz IS NULL OR s.starts_at < $5::timestamptz)
    GROUP BY u.id, u.name
), ranked AS (
    SELECT user_id, name, total_metres,
           rank() OVER (ORDER BY total_metres DESC)::bigint AS position
    FROM totals
)
SELECT ranked.user_id, ranked.name, ranked.total_metres, ranked.position
FROM ranked CROSS JOIN params
WHERE ranked.position <= 10 OR ranked.user_id = params.current_user_id
ORDER BY ranked.position, lower(ranked.name), ranked.user_id
`

type ListDistanceLeaderboardParams struct {
	CurrentUserID uuid.UUID          `json:"current_user_id"`
	ActiveOn      pgtype.Date        `json:"active_on"`
	AsOf          pgtype.Timestamptz `json:"as_of"`
	PeriodStart   pgtype.Timestamptz `json:"period_start"`
	PeriodEnd     pgtype.Timestamptz `json:"period_end"`
}

type ListDistanceLeaderboardRow struct {
	UserID      uuid.UUID `json:"user_id"`
	Name        string    `json:"name"`
	TotalMetres int64     `json:"total_metres"`
	Position    int64     `json:"position"`
}

func (q *Queries) ListDistanceLeaderboard(ctx context.Context, arg ListDistanceLeaderboardParams) ([]ListDistanceLeaderboardRow, error) {
	rows, err := q.db.Query(ctx, listDistanceLeaderboard,
		arg.CurrentUserID,
		arg.ActiveOn,
		arg.AsOf,
		arg.PeriodStart,
		arg.PeriodEnd,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListDistanceLeaderboardRow{}
	for rows.Next() {
		var i ListDistanceLeaderboardRow
		if err := rows.Scan(
			&i.UserID,
			&i.Name,
			&i.TotalMetres,
			&i.Position,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
