# Validation and CLI errors are deliberately localized, user-facing sentences.
# Go's ST1005 convention would make those messages less useful in Portuguese.
#
# Package comments predate this gate and generated packages cannot be edited.
# Add comments incrementally when packages gain useful public documentation.
#
# filippo.io/csrf keeps TemplateField as a no-op compatibility API. Removing the
# fields is a separate templ/API cleanup because it touches every form view.
checks = ["all", "-ST1000", "-ST1005", "-SA1019"]
