# Acceptance evidence matrix

This is the phase-10 status for the implemented application slice. A row is only marked covered where an executable test exists; manual checks and unavailable environments remain explicit.

| Workflow | Unit evidence | Integration evidence | Browser evidence | Status / gap |
|---|---|---|---|---|
| Retired privacy automation (#318) | Router, handler, config, release-receipt and host-retirement tests prove there is no in-app intake, reviewer, worker, activation, restore, purge or scheduled-retention runtime | `TestPrivacyAutomationRetirementIsPermanentAndPreservesEvidence` verifies the forward-only database fence, revoked web table/routine access, removed inbound/outbound legacy memberships, `NOLOGIN` legacy roles and preserved historical tables | Legal-page and profile navigation coverage directs users to `/legal/direitos`; the former privacy-request journey is removed | Covered. Rights requests are manual by email or letter; live data changes require a separately approved runbook |
| Media upload cleanup (#318) | Coordinator, cryptography and command tests cover bounded claims, stable absence, retries and privacy-safe output | PostgreSQL and MinIO coverage exercises provenance-backed cleanup of replaced, removed, failed and abandoned media | Existing profile, repair and equipment photo journeys cover the user-facing lifecycle | Covered as general media maintenance with a narrow database role and prefix-limited object identity |
| Manual data retention (#318) | One-shot command tests require the explicit enable switch and `--confirmed-manual-review`; the command uses a fixed bounded batch and emits aggregate output only | `TestRunUsesDedicatedLoginAndBoundedCapability` executes the successful path through a `NOINHERIT` login, verifies only the fixed `data_retention_*` API is callable and proves application-table reads remain denied | No browser or schedule exists | Covered as an operator-only manual action; owner, approval and aggregate evidence are required |
| Generic PostgreSQL restore verification (#318) | Shell coverage rejects disabled, unauthenticated and mutable-image configurations and exercises the successful authenticated selection, decrypt, isolated restore, migration and invariant-query path with command-boundary fakes | The annual host job uses an internal Docker network and disposable PostgreSQL container; a live retained-backup run remains a release operations gate | No browser surface exists | Source covered; enablement requires authenticated backup manifests and an immutable application image |
| Adult registration, login, session, consent validation | `internal/handlers/registration_test.go`, `login_test.go` | `internal/handlers/integration_test.go:TestPostgresRegistrationStorePersistsRequiredTermsConsentAtomically` | `e2e/auth.spec.mjs` registration and interactive login | Covered |
| Public legal information and optional sensitive-data consent | `internal/handlers/legal_test.go`, router, component, registration and profile tests cover the seven allowlisted current routes, representative immutable versioned routes, and both privacy versions with their exact digests, shared legal navigation, optional profile-photo authorization and explicit adult health-data consent | `TestPostgresProfileStoreEnforcesGuardianConsentConflictAndAudit` covers atomic health-consent evidence, refusal without consent or with a different hash, administrator minimization, majority cutoff and purpose-specific profile-photo consent | The legal page is covered at mobile/desktop widths; authentication and authenticated layouts expose the shared navigation; registration proves image authorization is absent and the photo flow requires it | Covered at the implemented points; verified authority for a representative to grant special-category/image consent remains deferred, so those grants are limited to the adult titular in this release |
| Password recovery | `internal/passwordreset/service_test.go`, `internal/emailverification/smtp_test.go`, `worker_test.go`, `internal/handlers/password_recovery_test.go`, auth/session tests, and router/security-header tests cover generic outcomes, privacy-safe operational events, trusted-client limiting, validation, replay, one-time confirmation, credential-version rejection, CSRF, anonymous-only routing, and privacy headers | PostgreSQL coverage exercises concurrent issuance, outbox creation, exactly-once consumption, bcrypt replacement, credential-version increments, old-session rejection, resend limits, administrator/minor credential replacement, and eligibility cancellation | `e2e/auth.spec.mjs` follows the Mailpit link, checks validation without password repopulation, resets through the browser flow, rejects the old password/session/used link, accepts the new password, and covers generic unknown/`CFC-`, axe, keyboard, and 320px states | Covered; operator diagnosis is documented in `docs/password-recovery-operations.md` |
| Guardian authority, dependants and minor credentials (#277) | `guardian_test.go`, `dashboard_test.go`, `auth_test.go`, member-handler, router and profile-store tests cover policy closure, status-only pending disclosure, explicit verifier capability, administrator non-inheritance, self-decision refusal, terminal/majority read-only states, closed evidence/reason vocabularies, validation, optimistic concurrency, credential cutoff and current-authority profile enforcement | Guardian-authority database tests cover no-policy/no-grant defaults, audited policy and grant changes, declaration, verification, suspension, rejection, effective and explicit expiry, majority, automated cutoff reconciliation, dormant-credential/session destruction, conflict, verifier-grant concurrency, immutable history, protected administrator mutations and guardian-derived query enforcement; a predecessor-schema migration test proves legacy backfill provenance, pointer/credential/session removal and fail-closed disclosure | `e2e/auth.spec.mjs` follows status-only pending disclosure, a no-JavaScript 320px invalid verifier decision with focused errors, policy-bound verification, post-verification profile access, conflict suspension and revoked access, resolution by a different verifier, terminal rejection and expired status; the five states receive responsive and serious/critical axe evidence across the lifecycle | Covered for the one-current-guardian model. Fresh databases intentionally have no policy or grants; policy adoption/activation and verifier grants use controller-only database APIs with no administrator browser UI. Formal transfer, multiple concurrent guardians, controller-approved evidence custody/retention and the adult-account handoff at majority remain outside this slice |
| Guardian-authority V2 approval and operational activation (#280) | `cmd/guardian-activation` tests require root, the fixed operator login, database-enforced deterministic canonical actor-bound approval JSON with no trailing byte, exact nested policy bytes and hash, calendar dates, expected database, immutable image digest and embedded migration digest; executable, database/bootstrap and deployment tests prove offline generation, explicit separate role provisioning, allowlisted modes/files, active-image equality, aggregate output and that routine release uses only a dedicated disable-and-bind login | PostgreSQL tests prove wrong-schema, wrong-database and wrong-image refusal, all verified/suspended relationship plus dangling credential/session inventory blocking, exact canonical-byte rejection, true separate-connection atomic enable/disable, exact repeated-enable idempotency, active-administrator binding, fixed operator and release-only function privileges through real logins, web-role denial, owner/table/introspection denial, emergency relationship expiry, comprehensive credential/session revocation, unbound-actor refusal, immutable events, the exact 004-to-005 fail-closed upgrade, and the transactional first-rollout sequence that stages a connect-only release login before `guardian_ops` exists then grants only the cutoff API after migration | No browser evidence is claimed because this is a root-only operational boundary; the existing #280 journeys remain disabled until a separate live activation gate | Source covered and production-inactive. Migration 005 advances the privacy evidence boundary, invalidates privacy/guardian activation and leaves intake plus every policy disabled. No real actor/reference/date/policy approval, login password, provisioning, activation, deployment or production check is supplied by this change. The runbook requires separate protected-input custody, preflight, human enable approval, aggregate CloudWatch verification, dedicated release-bind credential custody, image-bound release ordering and destructive fail-closed rollback. |
| Role workspaces and access control | `auth_test.go`, `dashboard_test.go` | `internal/db/integration_test.go:TestMembershipsResolveActiveSportStructureAndRejectMismatches` | Registration dashboard redirect and administrator fleet in `e2e/auth.spec.mjs` | Covered at unit/integration; coach/moderator browser journeys remain absent |
| Member management | `members_test.go` | None | Administrator creates an adult and assigns a competition membership in `e2e/auth.spec.mjs` | Covered for adult creation and membership assignment; dependant credentials and deactivation lack browser evidence |
| Private member profiles | `profile_test.go` covers authorization, validation, metadata-only audit fields, initials fallback, FPC number validation, allowlisted national/international history URL derivation, and the explicit destructive photo-removal confirmation | `TestPostgresProfileStoreEnforcesGuardianConsentConflictAndAudit`, `TestMemberProfileOptimisticUpdateAndImmutableAudit`; MinIO object lifecycle remains covered by `internal/storage/integration_test.go` | Adult editing, profile completion, consent-backed photo upload and removal confirmation, administrator FPC assignment followed by athlete-visible official-history links, responsive/axe checks, and guardian dependant editing in `e2e/auth.spec.mjs` | Covered for self/guardian editing, consent, storage compensation, stale-write protection, sensitive audit metadata, authenticated avatars, inactive-member restrictions, destructive self-photo removal, and numeric-only FPC outbound links without profile scraping; dependant read-only, guardian/administrator photo-removal journeys, and administrator identity correction outside the FPC field remain without dedicated browser journeys |
| Events | `events_test.go` | `TestListEventsForTodayRespectsMembershipCoachGrantAndAdminVisibility`, `TestEventEditAndCancellationPreserveResponsesAndRejectStaleWrites` | Administrator authoring, editing/cancellation, RSVP, capacity/waitlist, confirmation, and check-in in `e2e/auth.spec.mjs` | Source and PostgreSQL coverage includes lifecycle, concurrency, preserved responses, and cancelled visibility; coach and guardian browser journeys remain absent |
| Past events (#140 extension) | `events_history_test.go` view defaults, paging, allowlisted return/subject-switch context | `TestMemberEventHistoryBoundaryPaginationAndCurrentMembership` exact end boundary, ongoing/future split, both event types, cancellation, ordering, pagination and expired-membership denial | `e2e/event-results.spec.mjs` archive navigation, authorized results link, return page, 320px/axe | Existing current guardian and audience predicates retained; no historical authority or new schema. |
| Competition FPC results links (#140) | `event_results_test.go` URL, authorization, validation draft and stale-version tests | `TestEventResultsLinksAuthorizationLifecycleAndIntegrity`, `TestEventResultsTypeChangeSerializesBothLockOrders`, `TestEventResultsMigrationFromPredecessor`, relational privacy inventory | `e2e/event-results.spec.mjs` covers completed-event add/replace/remove, keyboard navigation, validation and stale-write recovery, 320px layout and axe | Source supports administrator-only mutation with latest actor/time, authorized event reads, no-referrer outbound link and no fetching. PostgreSQL covers coach/inactive denial, stale removal, both production-query lock orders and migration activation fencing. Member/coach browser journeys remain without dedicated results-link coverage; imports/history/rankings remain deferred. |
| Announcements and official documents | `announcements_test.go` | None | Targeted publication, first-read delivery, and expiry in `e2e/auth.spec.mjs` | Covered for administrator and competition-athlete lifecycle; coach and official-document browser journeys remain absent |
| Member suggestions | `suggestions_test.go`, `auth_test.go` cover validation, requester identity, requester-scoped rendering, staff authorization, terminal responses, pagination filters, and stale-write handling | `TestSuggestionsRemainRequesterScopedAndUseOptimisticTriage` | `e2e/auth.spec.mjs` covers member submission, administrator triage, requester-visible response, axe, and an interactive member page | Covered for the bounded private suggestion lifecycle; dedicated moderator browser evidence remains absent |
| Feature availability | `internal/featureflags/featureflags_test.go`, `auth_test.go`, `dashboard_test.go`, and `feature_flags_test.go` cover registry defaults, the disabled/administrator-only/enabled matrix, fail-closed reads, navigation, validation, and stale writes | `TestFeatureFlagsUseDefaultsConcurrencyAndImmutableAudit` covers seeded defaults, optimistic updates, preserved feature data, repair of a missing registered row, and append-only actor audit | `e2e/feature-flags.spec.mjs` changes only the dormant photo-submission setting, verifies all modes and audit output, and restores the disabled default | Covered for registered server-rendered routes; Suggestions is enforced end-to-end and future slices must explicitly apply their registered guard |
| Private photo albums | `photo_albums_test.go`, `auth_test.go` cover scoped list rendering, explicit audience validation, moderator authoring, fail-closed detail, versioned archive, and the deliberate absence of upload controls | `TestPhotoAlbumIntegrationScopesLifecycleAndAudit` covers member, named-guardian, coach, unrelated-user, privileged, archive, required-audience, and immutable-audit behavior | `e2e/auth.spec.mjs` creates and archives an album, checks scoped athlete visibility and axe, and confirms that no upload control exists | Covered for the #149 album foundation only; consent-aware upload, moderation, private media delivery, reporting, removal, and retention remain #150-#152 |
| News | `news_test.go`, `dashboard_test.go` | None | Administrator draft/publish/expire and leisure-workspace visibility in `e2e/auth.spec.mjs` | Covered for past scheduled publication; future-time automatic publication remains untested |
| Repair reporting and status transitions | `repair_test.go`, `dashboard_test.go` | `TestCreateRepairRequestEnforcesIdempotencyKeyDuringConcurrentRetries` | `e2e/auth.spec.mjs` photo upload and same-key retry | Covered for hostile invalid upload, cross-user retry, database cleanup, concurrent duplicate creation and stale status conflict; administrator resolution lacks browser evidence |
| Maintenance scheduling and completion | `dashboard_test.go` | `TestScheduleMaintenanceTaskUpdatesOnlyDueEquipment` | `e2e/auth.spec.mjs` administrator validation, keyboard scheduling and completion | Covered |
| Training, structured planning, private publication, activity integration foundation, and distance leaderboard | `training_test.go`, `structured_training_test.go`, `ui/pages/structured_training_test.go`, `dashboard_test.go`, `internal/activity/provider_test.go` | `TestTrainingSessionEditingAndCancellationLifecycle`, `TestPostgresTrainingPublicationsPreservePrivateRevisionLineage`, `TestPostgresStructuredTrainingCyclesRemainScopedVersionedAndCopyIndependentDrafts`, structured group/week/season/hybrid hierarchy and guardian visibility integration coverage, `TestDistanceLeaderboardEnforcesRankingPrivacyAndOwnership`, `TestActivityIntegrationPersistsIdempotentlyAndOwnsMatches`, `TestActivityMigrationPreservesExistingRecords` | `e2e/auth.spec.mjs` covers ordinary plan/session authoring, future-session editing/cancellation, cancelled athlete visibility, athlete actual-duration/distance/perception reporting and correction, Today ranking/privacy, an administrator creating a season-labelled hybrid gym/water week, and the focused exact-preview → immutable publication → private detail journey with axe and 320px checks; responsive leaderboard states remain in `e2e/ui-review.spec.mjs`. | Covered for optional multi-week cycle creation/editing, scoped ordered-week assignment, competition targets, optimistic concurrency, honest load/modality coverage warnings, and independent deep copy without publications, outcomes or targets. Structured group/week/session/segment/block persistence, explicit season association, hybrid segment ordering, variation precedence/conflicts/omission, immutable per-athlete publication revisions, exact material-change preview, self/current-minor-guardian/current-coach authorization, stale publication rejection, historical readability after membership/group changes, outcome-to-prescription lineage, version-safe feedback correction, and exact-revision feedback visibility are also covered. Structured water prescriptions, revisioned intensity profiles, reusable routines, and provenance-labelled planned totals are covered; cycle authoring currently has handler/component/accessibility semantics but no dedicated Playwright journey. The multiple-minor guardian chooser has unit rendering/authorization evidence but no dedicated Playwright journey. Athlete feedback is covered in the focused publication journey and privacy rules are documented in `docs/training-feedback-privacy.md`; wearable-derived planned/realized load remains a separate ticket; provider-specific OAuth/sync, automatic matching, and Today confirmation UI remain separate work; replacement-session persistence still lacks browser evidence. |
| Validation and error responses | Handler tests including `login_test.go`, `registration_test.go`, `repair_test.go`, `dashboard_test.go` | Transaction rollback in registration integration test | Registration, maintenance and repair validation paths in `e2e/auth.spec.mjs` | Covered for implemented forms; error-summary focus still needs manual verification |
| Responsive, keyboard and accessibility | Component and handler rendering tests, including equipment validation, private photo upload and stale-edit recovery | Equipment CRUD audit, retirement cancellation, history preservation and reactivation in `internal/db/integration_test.go`; MinIO upload/delete in `internal/storage/integration_test.go` | `e2e/accessibility-states.spec.mjs` covers representative public empty/invalid, member empty, 403/404, administrator populated, and closed/open authoring states with structural and axe checks; `e2e/auth.spec.mjs` retains keyboard, responsive, zoom, upload and interactive journeys | Representative page-state regression coverage is in normal CI; screen-reader, contrast, exhaustive focus order, calendar fallback and non-representative route/data combinations remain manual per `docs/accessibility-manual-check.md` |

## Commands

| Evidence layer | Command | Environment |
|---|---|---|
| Unit | `make test` | Local Go toolchain |
| Database and object-storage integration | `make test-integration` | Docker, `.env`, PostgreSQL and MinIO |
| Browser and axe | `make test-e2e` | Docker, `.env`; starts the pinned Playwright container and seeded local stack |

`make verify-foundation` remains the faster deterministic focused gate. `make verify` runs the full available source gate, including generation, Go checks, browser coverage, and Terraform formatting/validation; environment-dependent and remaining acceptance gaps are still recorded below rather than treated as covered.
